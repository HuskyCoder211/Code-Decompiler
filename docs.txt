function calling: funct name, options/params;

varable definition: num, name, value; string, name, ", value, "; numarray, name, [num, num]; stringarray, name, [string, string];

function definition: funct, name, params object, code;

params definition: {, num, name, }; {, string, name, }; {, numarray, name, }; {, stringarray, name, };

return function: return, variable definition or use or function definition with return;

output to console/terminal: output, string; output, num;

input from console/terminal: input, string; input, num;

restart the program: restart;
