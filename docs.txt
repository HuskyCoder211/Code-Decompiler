function calling: funct name, options/params;

varable definition: num, name, value; string, name, ", value, "; numarray, name, [num, num]; stringarray, name, [string, string]; note: does not have to have assigned value.

function definition: funct, name, params object, code, end;

params definition: {, num, name, }; {, string, name, }; {, numarray, name, }; {, stringarray, name, };

return function: return, variable definition or use or function definition with return;

output to console/terminal: output, string, name; output, num, name;

input from console/terminal: input, string, name; input, num, name;

restart the program: restart;
